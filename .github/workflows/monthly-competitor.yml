name: Monthly Competitor Feed

on:
  schedule:
    - cron: '0 6 1 * *'   # 6am UTC 1st of month (2pm SGT)
  workflow_dispatch:
    inputs:
      markets:
        description: 'Comma-separated market slugs (default: all)'
        required: false
        default: 'mx,br,ph,id'

jobs:
  fetch:
    name: Fetch competitor product specs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: competitor_intel-main/package.json

      - name: Install dependencies
        working-directory: competitor_intel-main
        run: npm install

      - name: Run competitor feed fetcher
        working-directory: competitor_intel-main
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          MARKETS: ${{ github.event.inputs.markets || 'mx,br,ph,id' }}
        run: node scripts/fetchers/competitor-feed.js
